\system is most compelling when treated as a reproducible evaluation instrument instead of a one-off multilingual prompt dataset. The benchmark contribution is the grammar: a threat model, a leakage-resistant build discipline, and a tradeoff-aware evaluation harness that makes front-door prompt classification claims auditable.

The next milestone for this paper is empirical consolidation rather than conceptual expansion. Once the evaluation harness is frozen and the first end-to-end results land, the current draft should be able to support a benchmark paper with a clear production-facing thesis: adversarial prompt classifier evaluation should measure not only whether a wall blocks, but whether it blocks fairly, efficiently, and reproducibly.
