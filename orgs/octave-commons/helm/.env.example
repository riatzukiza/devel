# OpenCode Hub Configuration

# Root directory containing git repositories to discover and manage
ROOT_DIR=~/devel

# Port for the hub API server
HUB_PORT=4799

# OpenCode CLI configuration
OPENCODE_BIN=opencode
OPENCODE_ARGS=serve --port
OPENCODE_BASE_PORT=5900

# GitHub token for issue/PR indexing (optional but recommended)
GITHUB_TOKEN=ghp_xxx

# OpenAI API key for Pantheon agent planning
OPENAI_API_KEY=sk_xxx

# Persistence configuration
MONGODB_URL=mongodb://localhost:27017/opencode-hub
CHROMADB_PATH=./chroma-data

# Development mode
NODE_ENV=development