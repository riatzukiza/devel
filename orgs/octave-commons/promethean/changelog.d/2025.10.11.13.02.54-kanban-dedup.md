## Fix kanban pull duplication

- remove stale board entries when `pullFromTasks` syncs from task files
- treat UUID-form slugs as fallback values to prevent regenerated duplicates
- add regression test covering stale board duplicates replacing canonical tasks
- extend regression coverage for kanban CLI commands (`event-log`, `deduplication`, `push`, `sync`, `regenerate`)
