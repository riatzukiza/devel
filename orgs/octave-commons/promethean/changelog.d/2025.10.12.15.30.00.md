## Fixed

- Fixed TypeScript build errors in @promethean-os/worker package
- Resolved "noUncheckedIndexedAccess" TypeScript strict mode violations in layout.ts and snapshot.ts
- Added proper bounds checking and undefined handling for array access in:
  - `markChanged()` function in src/zero/layout.ts
  - `isChanged()` function in src/zero/layout.ts  
  - `applyLayout()` function in src/zero/snapshot.ts
- All tests now pass and typecheck succeeds