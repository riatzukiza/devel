# Add @promethean-os/autocommit package

- Created new workspace package `@promethean-os/autocommit` for automatic git commits with LLM-generated messages
- Implemented file watching with chokidar and debounced commits (10s default)
- Added OpenAI-compatible LLM integration with fallback to deterministic messages
- Configured for local Ollama (default: llama3.1:8b at localhost:11434/v1)
- Includes safety features: ignore patterns, diff size limits, dry-run mode
- CLI interface: `autocommit` with comprehensive configuration options
- Conventional Commits format with subject line ≤72 chars
- GPL-3.0-or-later license with proper headers
