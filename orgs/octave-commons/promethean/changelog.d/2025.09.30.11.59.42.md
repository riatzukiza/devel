- Implemented kanban task lifecycle helpers for creating, archiving, deleting, renaming, and updating tasks with persistence.
- Added AVA coverage for the new kanban task behaviors plus scenarios for pull/push/sync/regenerate flows.
- Enhanced `createTask` to support templates, body injection, and cross-linking of blocking relationships while updating affected tasks.
- Switched board serialization to reuse `MarkdownBoard` while preserving kanban settings footers.
