# 🚨 CRITICAL: Kanban System Crisis - Estimates Parsing vs Transition Mismatch

## Issue Summary

Discovered critical architectural bug where task estimates parsing works correctly but FSM transition rules cannot access the parsed estimates, causing complete workflow deadlock.

## Root Cause Analysis

1. **Task Parsing Works**: `taskFromFM` in kanban.ts correctly parses `estimates.complexity` from frontmatter
2. **Data Pipeline Intact**: Estimates preserved through `ensureLabelsPresent` → final task creation
3. **Transition System Broken**: FSM rules in `task-properly-broken-down?` receive empty `estimates: {}`
4. **System Deadlock**: Tasks stuck in breakdown column (14/13 limit) because complexity validation fails

## Technical Details

- **Parsing Path**: `readTasksFolder` → `parseFrontmatter` → `taskFromFM` → `ensureLabelsPresent` ✅
- **Transition Path**: FSM rules receive different data object or cached version ❌
- **Rule Failing**: `(and (:estimates task) (<= (get-in task [:estimates :complexity]) 5))`
- **Impact**: 14 tasks stuck in breakdown, WIP limits blocking entire workflow

## Evidence

- Debug logging shows: `estimates: { complexity: 3, scale: "", time_to_completion: "" }` ✅
- Transition validation sees: `estimates: {}` ❌
- Manual file edits with complexity: 3 still fail transition checks

## Next Steps Required

1. Identify disconnect between parsing and transition systems
2. Check for caching layers or data path divergence
3. Fix transition system to use same parsed task data
4. Validate fix with test task progression through full FSM

## Business Impact

- **CRITICAL**: Entire kanban workflow non-functional
- **BLOCKING**: No tasks can progress from breakdown → ready
- **URGENT**: Production system deadlock requires immediate fix

Status: 🔴 CRITICAL - System non-functional
