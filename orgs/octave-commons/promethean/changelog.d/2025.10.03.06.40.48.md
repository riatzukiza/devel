- Fix duck-web WebSocket and data channel helpers to avoid duplicate definitions and ensure mic capture initializes the worklet node before wiring listeners.
- Improve pcm16k worklet buffering, int16 clamping, and CLI capabilities while documenting Docker host resolution and using curl healthchecks.
