Generated tasks from labeled docs

- Added script `scripts/generate-tasks-from-labeled.mjs` to convert `docs/labeled/*.md` into kanban task files.
- Wrote 223 tasks to `docs/agile/tasks/from-labeled/` with front‑matter and summaries.
- Skipped board regeneration due to environment `pnpm` EPERM; run `pnpm kanban regenerate` locally to refresh the board.

