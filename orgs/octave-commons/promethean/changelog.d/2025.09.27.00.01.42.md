- fix `@promethean-os/report-forge` tests by running AVA against TypeScript sources and hardening render fallbacks.
- make `generateReport` emit repo metadata and accept read-only inputs while supporting optional abort signals.
