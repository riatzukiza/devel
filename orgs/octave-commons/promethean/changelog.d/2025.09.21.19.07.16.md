- make board tooling load configuration from env, JSON config, or CLI with repository auto-detection
- reuse the shared config for lints, indexer, and projector with docs/agile defaults and repo-level promethean.kanban.json
