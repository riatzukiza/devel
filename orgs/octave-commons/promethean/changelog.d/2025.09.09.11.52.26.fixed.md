DocOps UI: add ARIA roles to `<file-tree>` so Playwright can reliably locate items via `getByRole('treeitem')`, and add an offline-safe Markdown render fallback when the CDN `marked` script is unavailable. Stabilizes e2e `docops.e2e` and `chunk-hits.e2e` in restricted environments.
