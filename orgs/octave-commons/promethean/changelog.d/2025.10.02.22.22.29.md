- ensure the throttled RTC sender flushes waiters using a cloned queue to keep references immutable
- update the duck-web sender test to transpile the TypeScript helper on the fly so it runs without relying on a built .js artifact
