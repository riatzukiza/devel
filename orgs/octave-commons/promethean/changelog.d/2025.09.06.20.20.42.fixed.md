Fix build issues in @promethean-os/llm and @promethean-os/markdown-graph by adding a `js-yaml` type shim and switching broker service template import to `@promethean-os/legacy/serviceTemplate.js` with a shim. Verified both packages build.