# Summary
- Added a root pnpm script to run the consolidated test coverage suite and emit the combined report to `./coverage`.
- Ensured the coverage runner builds dependent workspaces and counts executed Promethean packages instead of filtering them out as node modules.
