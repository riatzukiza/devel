Refactor `@promethean-os/level-cache` to remove duplication via dependency-injected scope builder and high-order function; add AVA tests. Break `packages/docops/ui/index.html` into template partials with a lightweight loader, and adjust `main.js` to initialize after templates load.

