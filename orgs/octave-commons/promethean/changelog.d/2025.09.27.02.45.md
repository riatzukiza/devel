## Summary
- created `packages/frontends/` and relocated existing frontend packages
- updated tooling (pnpm, Nx, configs) and documentation to match the new structure
- logged follow-on agile tasks for backend, library, and tooling reorganizations
