## Fixed

- Prevented Nx lint targets from forwarding stray CLI flags to eslint so workspace lint commands succeed again.

- Extracted shared PCM16 downmix utilities into `@promethean-os/duck-audio` and adopted them in Cephalon and Duck Web to keep clamp and decimation behavior aligned.
