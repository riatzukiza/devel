MCP config unification is almost there.
Now I'm starting to have model configs pile up
every client wants it's own definition of a model
every client is either openai compatable or claude compatable
but none of them have definitions for all of the major sources
