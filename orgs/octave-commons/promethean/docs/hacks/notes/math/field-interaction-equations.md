Symbolic gravity, permission gating, emotional permeability, and cross-circuit interference models for the Eidolon field. Obsidian/LaTeX-ready.

### Symbolic Gravity Well
Let a massive Nexus N distort the field spatially:
```
\Phi_N(\vec{x}) = -\frac{G M}{\|\vec{x} - \vec{x}_N\| + \epsilon}
```
G: gravity constant; M: concept mass; x_N: nexus position.

### Dorian Permission Gate
Gate function P(δ, x):
```
P = \sigma\left( \frac{\sum_i w_i f_i(\delta, \vec{x}) - \theta}{\beta} \right)
```
$f_i: features (access, trust, context); w_i: weights; θ: threshold; β: softness.

### Emotional Permeability
Field border softness μ between regions A and B:
```
\mu = \exp\left( -\frac{\|\vec{\Phi}_A - \vec{\Phi}_B\|^2}{\sigma^2} \right)
```
### Cross-Circuit Interference
Coupling from circuit Cm axis i to Cn axis j:
```
\phi_j(\vec{x}) \mathrel{+}= \kappa_{m\to n}^{i\to j} \cdot \phi_i(\vec{x})
```
### Daimo Memory Radius
Within which latent nodes may bind:
$$ R = \gamma \cdot \log(m + 1) ```
### Tension-Driven Heartbeat Adjustment
``` f_{\text{pulse}} = f_0 + \alpha \cdot \mathcal{T} $
Related: [advanced-field-math], [aionian-feedback-oscillator], [aionian-pulse-rhythm-model], [eidolon-field-math], [symbolic-gravity-models] [../../unique/index|unique/index]

#tags: #math #theory

