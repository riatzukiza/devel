Compiler kit foundations: tiny tokenizer, Pratt parser to AST, ANF-like IR, lowering hooks, and room for optimization + codegen to JS/VM/WASM.

Suggested layout: `shared/js/prom-lib/compiler/` (common.ts, lexer.ts, ast.ts, parser.ts, …).

Related: [lisp-frontend-compiler-kit], [interop-and-source-maps] [../../unique/index|unique/index]

#tags: #compiler #parser #ir #codegen

