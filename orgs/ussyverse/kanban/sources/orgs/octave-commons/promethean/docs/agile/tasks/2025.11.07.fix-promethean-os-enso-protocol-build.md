uuid: "5001f11e-bfa6-4422-ad0f-00797474cee1"
title: "Fix @promethean-os/enso-protocol build"
slug: "fix-promethean-os-enso-protocol-build"
status: "todo"
priority: "P1"
labels: ["build-fix", "tsc", "enso"]
created_at: "2025-11-08T02:03:16Z"
estimates:
  complexity: ""
  scale: ""
  time_to_completion: ""
---

## Summary
- TypeScript compilation fails immediately because `config/tsconfig.base.json` cannot be read and dozens of downlevel/`import.meta` errors follow.
- Impact: `pnpm build` remains red for @promethean-os/enso-protocol.

## Failure Evidence
- tsc --project tsconfig.json reports TS5083: Cannot read file '/home/err/devel/orgs/riatzukiza/config/tsconfig.base.json'
- Secondary errors: missing `ErrorOptions`, MapIterator requires higher target, and `import.meta` not allowed with current module settings

## Required Actions
1. Point tsconfig extends/reference to the actual shared base file inside the repo
2. Raise compiler target/module or refactor to avoid ES2020 features
3. Confirm `pnpm --filter @promethean-os/enso-protocol build` passes

## Acceptance Criteria
- Build command `pnpm --filter @promethean-os/enso-protocol build` completes without errors
- Root `pnpm build` no longer reports this package as failing
- Tests or smoke checks relevant to @promethean-os/enso-protocol pass
