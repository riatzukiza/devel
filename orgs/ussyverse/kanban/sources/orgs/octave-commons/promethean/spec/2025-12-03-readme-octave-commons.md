# README org migration to octave-commons

## Prompt

Update `README.md` to reflect the repository move to the `octave-commons` organization.

## Relevant files

- `README.md` — clone instructions and org references (e.g., lines 20, 134, 190+).

## Existing issues / PRs

- Not checked; assumed none relevant for this quick doc update.

## Plan (phases)

1. Update Quick Start clone URL and any org references (`PrometheanAI` ➜ `octave-commons`).
2. Refresh submodule guidance to point to `octave-commons` org instead of `riatzukiza`.
3. Regenerate Remote READMEs list to point to `octave-commons/*` repositories.
4. Scan README to confirm no remaining stale org references.

## Definition of done

- All README references to old orgs (`PrometheanAI`, `riatzukiza`) are updated to `octave-commons`.
- Submodule guidance and remote README links reflect `octave-commons` locations.
- No instructions mention cloning from deprecated orgs.

## Requirements / notes

- Keep content otherwise unchanged; only org references should change.
- Maintain existing formatting and auto-generated markers.
