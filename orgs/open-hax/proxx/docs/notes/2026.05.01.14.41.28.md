No, we don't start making contract kinds that we could not durive from
primatives.

we probably need a `:policy/outcome :sort` though I supose you could durive
that from `:reduce` it'd be kind of annoying to do though.

No what you need to do for me is construct a model-router.edn file that is
contractually equivolent to
