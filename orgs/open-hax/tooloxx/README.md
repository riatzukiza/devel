# tooloxx

Canonical Open Hax monorepo for MCP-related packages and services.

## Layout
- `packages/*` — shared MCP libraries and package-style tools
- `services/*` — MCP servers and HTTP runtimes

This repo is also nested inside the larger `~/devel` workspace, so the root workspace `pnpm-workspace.yaml` includes these paths directly.
