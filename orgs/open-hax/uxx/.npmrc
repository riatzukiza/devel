# Needed for shadow-cljs: it expects a classic, hoisted node_modules layout.
# With pnpm's default symlinked layout, shadow-cljs often can't resolve
# transitive dependencies (e.g. react-markdown -> devlop).
node-linker=hoisted
