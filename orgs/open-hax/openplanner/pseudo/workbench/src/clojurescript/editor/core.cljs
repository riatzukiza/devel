(ns editor.core
  "Core editor functionality from opencode-cljs-electron")

(defn init
  "Initialize the editor component"
  []
  (println "Editor initialized"))

(defn start
  "Start the editor"
  []
  (println "Editor started"))

(defn stop
  "Stop the editor"
  []
  (println "Editor stopped"))