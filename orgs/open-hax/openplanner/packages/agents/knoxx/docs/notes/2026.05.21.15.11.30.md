Write a clj kondo hook that warns when a route is defined with  `route!` or
(`.<method> app (fn [req reply] ...))  directly and advise to use defroute
from @backend/src/cljs/knoxx/backend/macros.cljc instead

@backend/src/cljs/knoxx/backend/infra/routes/auth.cljs  uses .<method app
@backend/src/cljs/knoxx/backend/infra/routes/contracts.cljs does something
totally different... it isn't nearly as messay as the others but worth noting.

@backend/src/cljs/knoxx/backend/infra/routes/users/admin.cljs  uses route
directly

and it's doin this thing wrappin em all in a function...Write a clj kondo hook that warns when a route is defined with  `route!` or
(`.<method> app (fn [req reply] ...))  directly and advise to use defroute
from @backend/src/cljs/knoxx/backend/macros.cljc instead

@backend/src/cljs/knoxx/backend/infra/routes/auth.cljs  uses .<method app
@backend/src/cljs/knoxx/backend/infra/routes/contracts.cljs does something
totally different... it isn't nearly as messay as the others but worth noting.

@backend/src/cljs/knoxx/backend/infra/routes/users/admin.cljs  uses route
directly

and it's doin this thing wrappin em all in a function...
