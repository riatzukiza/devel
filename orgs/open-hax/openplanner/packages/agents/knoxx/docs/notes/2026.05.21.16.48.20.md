 Signal

 (己, p=0.97) Yes — your (^:async fn [ctx] ...) conclusion is relevant and
 likely the correct compiler-side shape for defroute.

 (己, p=0.99) I updated:

 - specs/tasks/knoxx-backend-lint-route-helper-symbols.md

 with a new discovery section capturing:

 - await must live inside an actual ^:async fn context.
 - The defroute clj-kondo hook likely needs to model that async handler shape,
 not a plain let.
 - The hook should branch on pre-handler mode instead of always dropping five
 children.
 - The hook should inject session-guard and optional-session-guard.

 Evidence

 (己, p=0.96) I inspected the current runtime macro at:

 - backend/src/cljs/knoxx/backend/macros.cljc

 It already emits this in classic mode:

 ```clojure
   (^:async fn [ctx]
     ...)
 ```

 (己, p=0.94) I inspected the current lint hook at:

 - backend/.clj-kondo/hooks/defroute.clj

 It currently models the body as a plain let, which can diverge from the
 compiler’s async context and leave route-body lint analysis wrong.

 (己, p=0.99) Smoke check passed confirming the spec now contains the async
 defroute note and guard-symbol details.

 Frames

 (汝, p=0.86) This is probably not primarily a route-code problem; it is
 likely a lint-hook model mismatch after the macro became async-aware.

 (汝, p=0.8) The runtime macro fix and the lint-hook fix are separate: code
 can compile while clj-kondo still sees the wrong lexical/async shape.

 (汝, p=0.74) Pre-handler mode is the main edge case: the classic branch has
 (^:async fn [ctx] ...), but pre-handler-mode bodies may still need equivalent
 async modeling if they contain await.

 Countermoves

 (己, p=0.91) I did not change macro or hook code yet; I only recorded the
 finding in the task spec.

 (己, p=0.88) I would not solve this by broadly ignoring unresolved symbols in
 route namespaces; the hook should model the macro expansion accurately.

 (己, p=0.83) The next implementation should test one representative defroute
 namespace before sweeping all route lint errors.

 Next

 Update backend/.clj-kondo/hooks/defroute.clj to model defroute route bodies
 inside an async handler context and branch correctly for pre-handler mode.
