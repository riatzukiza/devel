 Signal

 (己, p=0.98) You were right. I corrected the model:

 - Drivers are ClojureScript code/protocol implementations.
 - Drivers are not EDN resources.
 - Sources are EDN resources.
 - A source binds:
     - one driver implementation
     - one actor/credential owner
     - the event types that actor wants admitted via :source/listens

 (己, p=0.95) The working shape is now:

 ```clojure
   {:contract/kind :source
    :contract/id "discord_gateway"
    :source/id :source/discord-gateway
    :source/type :event-generator
    :source/driver :driver/discord
    :source/actor "discord_automation"
    :source/listens [:discord.message
                     :discord.message.mention
                     :discord.message.created
                     :discord.voice.state-update]}
 ```

 (己, p=0.95) The event path is now real and explicit:

 ```clojure
   (source-runtime/dispatch-driver-event!
    config
    :driver/discord
    "discord_automation"
    {:event/type :discord.message
     :event/payload {...}})
 ```

 That does:

 ```clojure
   driver + actor + event-type
   -> find enabled source with same :source/driver, :source/actor, and
 :source/listens
   -> attach provenance:
      {:event/actor "discord_automation"
       :event/generator {:kind :discord
                         :driver :driver/discord
                         :source :source/discord-gateway}}
   -> event-dispatch/dispatch!
   -> matching triggers run actions
 ```

 (己, p=0.94) Added code-level drivers for:

 - :driver/discord
 - :driver/eta-mu-ingestion
 - :driver/agents
 - :driver/knoxx
 - :driver/user
 - :driver/org
 - :driver/sessions
 - :driver/translations

 (己, p=0.93) Added/updated source resources for actor-owned instances:

 - contracts/sources/discord_gateway.edn
 - contracts/sources/discord_voice_audio.edn
 - contracts/sources/eta_mu_sessions.edn
 - contracts/sources/agent_runtime.edn
 - contracts/sources/knoxx_runtime.edn
 - contracts/sources/user_events.edn
 - contracts/sources/org_events.edn
 - contracts/sources/session_events.edn
 - contracts/sources/translation_events.edn

 (己, p=0.92) I removed the wrong direction: no contracts/drivers resource
 model remains.

 Evidence

 (己, p=0.95) Key implementation files:

 - backend/src/cljs/knoxx/backend/domain/driver/registry.cljs
 - backend/src/cljs/knoxx/backend/domain/driver/builtin.cljs
 - backend/src/cljs/knoxx/backend/domain/source/runtime.cljs
 - backend/src/cljs/knoxx/backend/domain/control/catalog.cljs
 - backend/src/cljs/knoxx/backend/infra/event_runtime.cljs
 - backend/src/cljs/knoxx/backend/infra/core.cljs

 (己, p=0.94) Regression coverage:

 - backend/test/cljs/knoxx/backend/source_runtime_test.cljs
 - backend/test/cljs/knoxx/backend/registry/resource_registry_test.cljs
 - backend/test/cljs/knoxx/backend/triggers/control_config_test.cljs

 (己, p=0.95) Verification:

 - Focused clj-kondo: 0 errors, 0 warnings
 - git diff --check: passed on touched files
 - pnpm -C backend test: 389 tests, 1071 assertions, 0 failures, 0 errors
 - pnpm -C backend exec shadow-cljs compile server: exit 0
 - Existing broad compiler warnings remain unrelated.

 (己, p=0.93) Receipt appended with DOD
 code-drivers-source-listens-event-path.

 Frames

 (己, p=0.92) Frame 1: The useful boundary is now “source = actor-owned driver
 instance,” not “source = event shape author.”

 (己, p=0.9) Frame 2: Multiple Discord bots now fall out naturally: add
 multiple source resources with the same :source/driver :driver/discord but
 different :source/actor values.

 (己, p=0.87) Frame 3: Hooks replacement becomes: runtime systems emit driver
 events, source resources admit them, triggers react to them, actions do the
 work.

 Countermoves

 (己, p=0.91) I did not keep driver specs as EDN resources; that was the wrong
 abstraction.

 (己, p=0.88) I did not make sources define event shapes; :source/emits is now
 rejected by catalog admissibility for event sources.

 (己, p=0.86) I did not restart Knoxx PM2 processes.

 Next

 (己, p=0.9) Replace remaining direct/manual event dispatch call sites with
 source-runtime/dispatch-driver-event! so every system event enters through an
 actor-owned source.
