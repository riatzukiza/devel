#!/usr/bin/env bash
set -euo pipefail
# Usage: giga-commit <subRepoPath> <action> <result> [files...]
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
ROOT="$(cd "${SCRIPT_DIR}/.." && pwd)"
cd "$ROOT"
exec bun run src/giga/commit-propagator.ts "$@"
