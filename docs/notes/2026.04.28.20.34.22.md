### User

but now I have to update everything that was USING it like knoxx, no, the two modes don't coexist, so there should be no problem with them having the same port.... hmmm.... "existing cljs warnings..." yea we made that code like 10 minutes ago, and I already know what is gonna happen if we let those warnings pile up, cause most of them aren't really things to ingore. "unbound variable" is a very dangerous thing to allow go under the radar.

### Assistant
 Signal

 (己, p=0.96) Agreed. I treated the CLJS warnings as defects, fixed them, and added tests.

 Fixed:

 ```text
   packages/stores/document-hydration/src/openplanner/stores/document_hydration.cljs
 ```

 The warning source was the Redis adapter:

 ```clojure
   (.del client ...)
   (.pExpire client ...)
 ```

 I added JS type hints:

 ```clojure
   (deftype RedisCache [^js client prefix default-ttl-ms] ...)
   (deftype LmdbTtlCache [^js db prefix default-ttl-ms] ...)
 ```

 Also found and fixed a real bug while testing:

 (己, p=0.94) create-redis-cache and create-lmdb-cache were using deep js->clj option conversion, which converted
 injected JS clients/DB handles into CLJS maps. That would have caused exactly the kind of ghost runtime failure you’re
 warning about.

 Fixed by reading injected handles directly with JS property access:

 ```clojure
   (let [client (jget opts "client") ...])
   (let [db (jget opts "db") ...])
 ```

 Added CLJS tests:

 ```text
   packages/stores/document-hydration/test/openplanner/stores/document_hydration_test.cljs
 ```

 Added package test script:

 ```json
   "test": "shadow-cljs compile test && node target/test.cjs"
 ```

 Evidence

 (己, p=0.97) CLJS tests pass with zero warnings:

 ```bash
   pnpm --filter @open-hax/openplanner-document-hydration test
 ```

 Result:

 ```text
   [:test] Build completed. (50 files, 2 compiled, 0 warnings, 1.79s)

   Ran 5 tests containing 21 assertions.
   0 failures, 0 errors.
 ```

 (己, p=0.97) Full OpenPlanner build passes with zero CLJS warnings:

 ```bash
   pnpm build
 ```

 Result:

 ```text
   [:lib] Build completed. (47 files, 1 compiled, 0 warnings, 5.53s)
 ```

 Tests cover:

 - redacted row detection and hydration
 - document cache key derivation
 - row-to-document
 - memory LRU TTL + eviction
 - layered cache promotion from warm layer to hot layer
 - Redis adapter delegation against a fake JS client
 - LMDB adapter delegation/TTL against a fake JS DB handle

 Commits:

 ```text
   529e8b3 Test CLJS hydration cache adapters
   b831c539 Update openplanner submodule (hydration cache tests)
 ```

 Frames

 (己, p=0.9) Your warning was correct: the “warning” exposed a class of runtime bug, and the test caught an actual
 injected-client corruption issue.

 (己, p=0.84) The important invariant now is: CLJS cache adapters must preserve JS handles as JS handles. No deep js->clj
  over live clients.

 (己, p=0.8) This should become the house rule for CLJS interop: zero infer warnings unless there is a documented
 exception, and adapters get fake-client tests.

 Countermoves

 (己, p=0.86) I did not touch unrelated OpenPlanner state:

 ```text
    M packages/agents/knoxx
   ?? docs/notes/2026.04.28.15.06.02.md
   ?? docs/notes/2026.04.28.17.09.48.md
   ?? docs/notes/2026.04.28.18.02.51.md
 ```

 (己, p=0.82) Root still shows the OpenPlanner submodule as modified because of that unrelated internal state, not
 because this work is uncommitted.

 Next

 (己, p=0.88) Add pnpm --filter @open-hax/openplanner-document-hydration test to the OpenPlanner build/test path so CLJS
 warning/test regression is caught automatically.
