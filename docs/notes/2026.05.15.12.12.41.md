## Concrete next move

The next useful artifact is a **scoring schema** with:
- claim types,
- domain weights,
- confidence derivation rules,
- ambiguity classes,
- audit triggers,
- and per-domain rubric thresholds.

---

1. a JSON schema for evidence and scores,
2. a weighting formula section for the paper,
3. a human-audit rubric for contested clips,
4. or all three in one integrated spec.

---

Next, the most useful artifact is probably one of these:
1. a polished Mermaid diagram plus legend for your article,
2. a JSON Schema for every handoff packet,
3. a rubric table for Scribe mode and Composition mode,
4. or a state-machine spec with transitions and restart rules.
