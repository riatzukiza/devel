I think some context about what eta-mu actually is will help.
The cljs lisp extensions are... it at somepoint did work but we refored many of them.
They need to use the `(em/* ...)` macros to create tools and such.
Look at the ones that you actually hvae tools loaded from
