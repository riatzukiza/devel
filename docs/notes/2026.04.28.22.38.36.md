How do the "workers" work? What I kinda think is... the most valuable thing in knoxx is the contract runtime to compose agents from reusable contract components, initially I was imagining open planner as super set of the opencode API so users could easily migrate from the opencode sdk, to our more feature rich SDK with built in graph memory and stuff.

Ideally, the opencode TUI client could connect to the open planner eco system and not know the difference except the agents are composed from contracts.

Does it use opencode? or the pi sdk? Or something else?
