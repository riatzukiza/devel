# Task Master (.opencode) package

This folder is a ready-to-drop `.opencode/` config containing:
- `agents/task-master.md`
- `skill/<skill-name>/SKILL.md` for all meta/state/transition skills
- `docs/` supporting docs

Install by copying the zip contents into your repo's `.opencode/`.
