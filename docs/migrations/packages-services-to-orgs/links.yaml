# Managed workspace aliases.
#
# `from` is a stable operator/developer path kept for compatibility.
# `to` is the current runtime or canonical target inside this workspace.

links:
  - from: services/open-hax-openai-proxy
    to: services/proxx
