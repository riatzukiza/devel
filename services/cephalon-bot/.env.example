# === Bot selection ===
# Each process should set a bot id
CEPHALON_BOT_ID=duck

# === Discord tokens (match config/bots.ts) ===
DUCK_DISCORD_TOKEN=your_token_here
JANITOR_DISCORD_TOKEN=your_token_here
# Or set DISCORD_TOKEN for quick testing.

# === Persistence (optional but recommended) ===
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB=cephalon

# === Chroma (optional) ===
CHROMA_URL=http://localhost:8000
CHROMA_COLLECTION=cephalon_memories

# === Optional UI ===
MEMORY_UI_PORT=3000
