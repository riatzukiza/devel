apiVersion: v1
kind: Service
metadata:
  name: openplanner
spec:
  selector:
    app.kubernetes.io/name: openplanner
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 80
      targetPort: http