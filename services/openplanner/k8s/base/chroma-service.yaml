apiVersion: v1
kind: Service
metadata:
  name: openplanner-chroma
spec:
  selector:
    app.kubernetes.io/name: openplanner
    app.kubernetes.io/component: chroma
  ports:
    - name: http
      port: 8000
      targetPort: http