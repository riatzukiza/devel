#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PKG_ROOT="$(cd "${SCRIPT_DIR}/.." && pwd)"

if [[ ! -f "${PKG_ROOT}/dist/index.js" ]]; then
  (cd "${PKG_ROOT}" && bun run build >/dev/null)
fi

cd "${PKG_ROOT}"
exec bun "./dist/index.js" -- "$@"
