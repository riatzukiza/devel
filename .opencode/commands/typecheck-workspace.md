# OpenCode Command: typecheck-workspace

```yaml
name: typecheck-workspace
description: Type check all TypeScript files across the workspace
usage: |
  ## Usage
  pnpm typecheck-workspace

  ## Examples
  pnpm typecheck-workspace
  pnpm typecheck-workspace --only ts
  pnpm typecheck-workspace --verbose
```
