(handoff
  (kind eta-mu-kanban-migration)
  (batch agent_open_hax_other)
  (time "20260529T040601Z")
  (manifest "/tmp/eta-mu-kanban-batches/agent_open_hax_other.json")
  (verification "eta-mu-beta kanban count --tasks-dir for every unique boardDir")
  (concurrency "path-scoped staging only; unrelated dirt untouched"))
