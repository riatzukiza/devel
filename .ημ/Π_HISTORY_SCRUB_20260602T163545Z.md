# Secret history scrub receipt — 20260602T163545Z

Target refs:

- refs/heads/main
- refs/heads/staging
- refs/heads/pi/fork-tax/20260529T022118Z-main-softreset-all-dirt

Actions:

1. Replaced redacted token/API-key/password values in reachable text blobs with `REDACTED_SECRET`.
2. Removed binary artifacts that retained old redacted values after text replacement:
   - `inbox/threat-radar-next-step.zip`
   - `tmp/openai_proxy-core-pre-reindex.dump`
3. Replaced the remaining redacted testing/prod `.envrc` token value.
4. Preserved no raw values in receipts, manifests, shell output, or committed artifacts.

Verification before promotion:

- Exact old-value scan, values length >= 12: zero hits across rewritten main, staging, and branch histories after binary purge.
- Redacted scanner artifacts: `/tmp/devel-history-secret-scan-after-scrub.json`, `/tmp/devel-secret-replacements-manifest-redacted.json`, and `/tmp/devel-secret-replacements-2-manifest-redacted.json`.

Operational note: rotate/revoke affected external credentials even after history rewrite.
